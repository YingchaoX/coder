# coder 使用指南

> **离线终端编码代理 · 单二进制 · TUI 开发体验**

`coder` 是一个专为开发者设计的离线终端编码代理，提供完整的工具调用、上下文管理、安全策略和多代理工作流能力。

---

## 🚀 快速入门

### 1. 环境准备

确保已安装 Go 1.21+：
```bash
# 检查 Go 版本
$ go version
# 推荐：Go 1.21 或更高版本
```

### 2. 构建二进制

```bash
# 构建主程序
$ go build -o bin/agent ./cmd/agent

# 构建跨平台版本（可选）
$ GOOS=linux GOARCH=amd64 go build -o bin/agent-linux-x86_64 ./cmd/agent
$ GOOS=darwin GOARCH=arm64 go build -o bin/agent-darwin-arm64 ./cmd/agent
```

### 3. 启动代理

```bash
# 使用示例配置启动
$ ./bin/agent -config agent.config.json.example

# 指定工作目录启动
$ ./bin/agent -config agent.config.json.example -cwd /path/to/your/project

# 从当前目录启动（默认工作区）
$ ./bin/agent -config agent.config.json.example
```

---

## 🧭 核心概念

| 概念 | 说明 |
|------|------|
| **会话 (Session)** | 独立的工作单元，支持创建、切换、分叉和回滚 |
| **代理 (Agent)** | 不同角色的智能体：`build`（默认）、`plan`（只读规划）、`general`（泛化探索）、`explore`（安全浏览） |
| **工具 (Tools)** | 内置功能模块，支持文件操作、系统命令、任务管理等 |
| **MCP** | Model Control Protocol，支持本地模型服务集成 |
| **技能 (Skills)** | 通过 `SKILL.md` 文件定义的可扩展功能模块，支持按需加载 |

---

## 🛠️ 基本操作

### 1. REPL 命令

| 命令 | 说明 | 快捷方式 |
|------|------|----------|
| `/help` | 显示帮助信息 | — |
| `/new` | 创建新会话 | — |
| `/sessions` | 列出所有会话 | — |
| `/use <id>` | 切换到指定会话 | — |
| `/fork <id>` | 从指定会话分叉新会话 | — |
| `/revert <n>` | 回滚到指定轮次（最近 n 条消息） | — |
| `/agent <name>` | 切换代理类型 | — |
| `/models [model]` | 查看或切换模型 | — |
| `/context` | 查看当前上下文状态（估算 token 使用量、限制和利用率） | — |
| `/tools` | 查看可用工具列表 | — |
| `/todo` | 查看当前待办事项（以 `[ ]`、`[~]`、`[x]` 格式显示） | — |
| `/summarize` | 生成会话摘要 | — |
| `/compact` | 手动压缩上下文 | — |
| `/config` | 查看当前配置 | — |
| `/mcp` | 查看 MCP 服务器状态 | — |
| `/exit` | 退出程序 | — |

### 2. 直接 Shell 模式

以 `!` 开头的命令直接执行 shell 命令，并记录在会话历史中：

```bash
! ls -la
! cat README.md
! grep -n "func main" *.go
! git status
```

> ⚠️ **安全提示**：即使使用 `!` 前缀，危险命令（如 `rm`、`mv` 等）仍需人工确认。

### 3. 文件路径引用

在提示中使用 `@path` 语法自动展开为文件内容（沙箱内）：

```
请分析 @main.go 和 @README.md 的一致性
```

---

## 🧰 工具系统

### 内置工具概览

| 工具 | 描述 | 默认权限 | 典型用途 |
|------|------|----------|----------|
| `read` | 读取文件内容 | `allow` | 查看源码、配置文件 |
| `write` | 写入/替换文件 | `ask` | 创建/修改代码文件 |
| `list` | 列出目录内容 | `allow` | 浏览项目结构 |
| `glob` | 文件模式搜索 | `allow` | 查找特定类型文件 |
| `grep` | 内容搜索 | `allow` | 在代码中搜索关键词 |
| `patch` | 应用统一差异补丁 | `ask` | 安全地应用代码变更 |
| `bash` | 执行 shell 命令 | `ask` | 运行构建、测试等命令 |
| `todoread`/`todowrite` | 待办事项管理 | `allow` | 复杂任务跟踪 |
| `skill` | 技能管理 | `ask` | 加载和使用自定义技能 |
| `task` | 子代理任务 | `ask` | 分布式任务处理 |

### 工具使用示例

**读取并分析文件：**
```
请读取 @main.go 并解释其主要功能
```

**搜索代码模式：**
```
在所有 .go 文件中搜索 "func.*Test"，找出测试函数
```

**创建新文件：**
```
创建 hello.go，内容：
package main

import "fmt"

func main() {
    fmt.Println("Hello, World!")
}
```

**应用代码补丁：**
```
请为 @main.go 应用以下补丁：
diff --git a/main.go b/main.go
index abc123...def456 100644
--- a/main.go
+++ b/main.go
@@ -1,3 +1,4 @@
 package main
 
 import "fmt"
+
 func main() {
```

**写操作结果可视化：**
- `write` 工具结果会显示操作类型（`created`/`updated`/`unchanged`）和变更摘要（`additions`/`deletions`）
- 大文件编辑会显示结构化 diff 预览而非原始 JSON

---

## 🎨 输出格式与可视化

### 输出分层
`coder` 使用三种输出类型来提高可读性：

- `PLAN`: 助手中间过程说明（当后续仍有 tool calls 时显示）
- `TOOL`: 工具开始、结果、错误、阻断信息
- `ANSWER`: 最终回复文本（无后续 tool calls 时显示）

### ANSI 颜色支持
- 默认启用 ANSI 颜色以区分 `PLAN`/`TOOL`/`ANSWER` 输出
- 可通过环境变量禁用：`NO_COLOR=1` 或 `AGENT_NO_COLOR=1`

### 流式响应
- 助手文本增量渲染：首个文本 chunk 到达时立即显示 `ANSWER` 区块
- 后续 chunk 按到达顺序追加到同一输出块
- 中断时保留已输出内容并显示结构化错误

---

## 🔒 安全策略

### 路径沙箱
- 所有文件操作限制在启动时的工作目录内
- 符号链接逃逸被阻止
- 绝对路径和 `..` 路径越界访问会被拒绝

### 危险命令审批
以下操作总是需要人工确认：
- 文件系统破坏性操作：`rm`, `mv`, `chmod`, `chown`, `dd`, `mkfs`
- 系统管理命令：`shutdown`, `reboot`, `systemctl`
- 重定向覆盖：`>`, `1>`, `2>`（当目标文件存在时）

### 权限模型
- `allow`: 自动执行，无需确认
- `ask`: 显示预览并等待人工确认
- `deny`: 永远拒绝，无法绕过

### 中文兼容性
- 默认使用与用户相同语言回复（除非用户另行要求）
- 支持多种响应格式解析（纯字符串、typed parts、嵌套对象）

---

## 🤖 代理系统

### 内置代理类型

| 代理 | 特点 | 最佳场景 |
|------|------|----------|
| `build` | 默认代理，完整工具集 | 日常开发、代码修改、构建任务 |
| `plan` | 只读代理，禁用写操作 | 代码分析、架构设计、方案规划 |
| `general` | 泛化探索代理 | 信息收集、文档阅读、广泛搜索 |
| `explore` | 强只读探索代理 | 安全敏感的代码审计、漏洞分析 |

### 代理切换

```bash
# 切换到规划模式
/agent plan

# 切换回构建模式
/agent build

# 查看当前代理状态
/agent
```

---

## ⚙️ 配置管理

### 配置文件

配置文件支持 JSON/JSONC 格式，主要配置项：

- `provider`: 模型服务配置（OpenAI 兼容）
- `runtime`: 运行时设置（超时、重试等）
- `permission`: 权限策略配置
- `agents`: 代理配置（默认代理、能力限制）
- `mcp`: MCP 服务器配置
- `storage`: 存储配置（会话持久化）

### 环境变量覆盖

| 环境变量 | 用途 | 示例 |
|----------|------|------|
| `AGENT_CONFIG_PATH` | 配置文件路径 | `./agent.config.json` |
| `AGENT_BASE_URL` | 模型服务地址 | `http://localhost:8000/v1` |
| `AGENT_MODEL` | 模型名称 | `qwen2.5-7b` |
| `AGENT_API_KEY` | API 密钥 | `sk-...` |
| `DASHSCOPE_API_KEY` | 百炼 API 密钥（备用） | `sk-...` |
| `AGENT_WORKSPACE_ROOT` | 工作区根目录 | `/path/to/project` |
| `AGENT_MAX_STEPS` | 最大执行步骤 | `50` |
| `AGENT_CACHE_PATH` | 缓存路径 | `./cache` |
| `NO_COLOR` / `AGENT_NO_COLOR` | 禁用 ANSI 颜色 | `1` |

---

## 🚀 高级工作流

### 1. 复杂任务管理

使用待办事项系统管理多步骤任务：

```bash
# 创建重构任务
请为这个重构任务创建待办事项：
1. 分析现有代码结构
2. 设计新的模块划分
3. 实现核心功能
4. 添加测试用例
5. 文档更新
```

> ✅ **Todo 渲染格式**：`/todo` 命令显示为 `[ ]`（待办）、`[~]`（进行中）、`[x]`（已完成）

### 2. 上下文管理

- `/context`: 查看当前上下文长度估算（`estimated_tokens`、`context_limit`、`usage_percent`）
- `/compact`: 手动触发上下文压缩（保留摘要和关键工具输出）
- 自动压缩：当上下文接近限制时自动触发

### 3. MCP 集成

支持本地 MCP 服务器，用于：
- 扩展模型能力
- 集成外部工具
- 多模型协作

### 4. 技能系统

通过 `SKILL.md` 文件定义和管理可扩展技能：

- 技能发现：自动扫描项目和全局技能目录
- 按需加载：仅在需要时加载技能内容
- 权限控制：支持 `allow`/`ask`/`deny` 权限策略
- 使用示例：
  ```
  /skill list
  /skill load my-skill
  ```

---

## 🐞 故障排除

### 常见问题解决

| 问题 | 解决方案 |
|------|----------|
| **启动失败："load config failed"** | 检查 `-config` 参数路径，或设置 `AGENT_CONFIG_PATH` 环境变量 |
| **工具调用无响应** | 检查网络连接（模型服务），或使用 `/models` 切换可用模型 |
| **权限拒绝** | 使用 `/config` 查看权限设置，或临时调整配置文件 |
| **上下文过长** | 使用 `/compact` 压缩，或增加 `AGENT_MAX_CONTEXT_LENGTH` 配置 |
| **Shell 命令被拒绝** | 检查是否为危险命令，确认后重试 |
| **文件操作失败** | 确认路径在工作区沙箱内，检查文件权限 |

### 调试技巧

- 使用 `/context` 查看实时上下文状态
- 使用 `/config` 验证配置加载
- 使用 `/tools` 确认工具可用性
- 查看日志输出（添加 `-v` 参数启用详细日志）

---

## 📜 附录

### 构建参考

```bash
# 静态链接 Linux 版本
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -a -ldflags '-extldflags "-static"' -o bin/agent-linux-x86_64-static ./cmd/agent

# macOS Apple Silicon 版本
GOOS=darwin GOARCH=arm64 go build -o bin/agent-darwin-arm64 ./cmd/agent
```

### 许可证

本项目采用 MIT 许可证。

### 贡献指南

欢迎提交 Issue 和 Pull Request。请遵循：
- 提交清晰的问题描述和复现步骤
- 对于功能请求，说明具体使用场景
- 代码变更需包含相应测试
- 文档更新保持一致风格

---

> **文档版本**: 3.1  
> **最后更新**: 2026-02-08  
> **项目主页**: [coder GitHub](https://github.com/your-repo/coder)
